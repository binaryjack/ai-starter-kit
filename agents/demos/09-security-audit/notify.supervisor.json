{
  "laneId": "notify",
  "retryBudget": 1,
  "checkpoints": [
    {
      "checkpointId": "step-0",
      "mode": "read-contract",
      "waitFor": "risk-report",
      "expect": { "minFindings": 1 },
      "onFail": "RETRY",
      "retryInstructions": "Risk report contract was empty. Generate a generic critical-alert notification indicating that the audit completed but report retrieval failed."
    },
    {
      "checkpointId": "step-1",
      "mode": "self",
      "expect": { "minFindings": 1 },
      "onFail": "ESCALATE"
    }
  ]
}
