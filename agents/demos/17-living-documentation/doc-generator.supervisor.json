{
  "laneId": "doc-generator",
  "retryBudget": 1,
  "checkpoints": [
    {
      "checkpointId": "step-0",
      "mode": "read-contract",
      "expect": {
        "contractAvailable": true
      },
      "onFail": "ESCALATE"
    },
    {
      "checkpointId": "step-1",
      "mode": "self",
      "expect": {
        "allSectionsGenerated": true
      },
      "onFail": "RETRY",
      "retryInstructions": "Documentation output too large for single pass — re-run in chunked mode: split by domain (auth, users, orders, events) and generate each section separately, then merge into final docs structure"
    }
  ],
  "publishContract": {
    "key": "generated-docs",
    "outputKeys": ["generated-docs"]
  }
}
